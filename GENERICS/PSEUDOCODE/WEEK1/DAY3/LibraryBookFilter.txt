Define class Book:
    Attributes: title, author, year, rating

Create List<Book> books

Use stream filter:
    filteredBooks = books.stream()
        .filter(book.year > 2015 && book.rating > 4.5)
        .collect(toList())

Print filteredBooks
