Define generic class Transaction<T>:
    Attribute: paymentMethod of type T
    Method: processPayment():
        Print "Processing payment via " + paymentMethod

Define classes UPI, Card, NetBanking with toString()

In main:
    Create Transaction<UPI>, Transaction<Card>, Transaction<NetBanking>
    Call processPayment() for each
