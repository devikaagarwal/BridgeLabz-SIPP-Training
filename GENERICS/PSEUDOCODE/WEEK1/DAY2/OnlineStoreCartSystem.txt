Define class Item:
    Attributes: name, price

Create List<Item> cart

Function addItem(cart, item):
    Add item to cart

Generic method calculateTotal(List<? extends Item> cart):
    total = 0
    For each item in cart:
        total += item.price
    Return total

In main:
    Add items to cart
    Call calculateTotal(cart)
    Print total price
